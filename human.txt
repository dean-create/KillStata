下一步做是真实论文数据+模型工具调用回放的验收基座，以自定义数据集即以上两个excel文件、linearmodels自带的数据集和根据计量方法自动查找的数据集（例如PSM要用NSW做专门基准，不能拿Card代替就去查找NSW）为依据，来验证以下三点：
1、工具真的被模型调用：
模型按JSON Schema生成参数；
Harness调到正确工具；
工具在真实论文数据上完成执行并返回结果。
2、数值实现正确
同一篇论文数据、同一模型设定，KillStata 的系数、样本量、标准误要和 linearmodels 或论文/权威实现对齐。
这才是算法层验证，不只是没报错。
3、模型选择对了方法
还要加真实模型调用回放，给 DeepSeek 数据画像和用户意图，检查它是否选对工具、变量角色和参数。

应把这些数据集固化成基础工具的真实论文验证和测试库。以后每个工具至少过：真实论文数据 → 固定参数调用 → Harness 执行 → 数值对标 → 失败参数拒绝 → 模型调用回放，这样才能证明不是模型嘴上会调用，而是真的能稳定做对。