name: test

on:
  push:
    branches:
      - dev
  pull_request:
  workflow_dispatch:
jobs:
  test:
    name: test (${{ matrix.settings.name }})
    strategy:
      fail-fast: false
      matrix:
        settings:
          - name: linux
            host: blacksmith-4vcpu-ubuntu-2404
            playwright: bunx playwright install --with-deps
            workdir: .
            command: |
              git config --global user.email "bot@killstata.ai"
              git config --global user.name "killstata"
              bun turbo typecheck
              bun turbo test
          - name: windows
            host: windows-latest
            playwright: bunx playwright install
            workdir: packages/app
            command: bun test:e2e:local
    runs-on: ${{ matrix.settings.host }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Install Playwright browsers
        working-directory: packages/app
        run: ${{ matrix.settings.playwright }}

      - name: Set OS-specific paths
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            printf '%s\n' "KILLSTATA_E2E_ROOT=${{ runner.temp }}\\killstata-e2e" >> "$GITHUB_ENV"
            printf '%s\n' "KILLSTATA_TEST_HOME=${{ runner.temp }}\\killstata-e2e\\home" >> "$GITHUB_ENV"
            printf '%s\n' "XDG_DATA_HOME=${{ runner.temp }}\\killstata-e2e\\share" >> "$GITHUB_ENV"
            printf '%s\n' "XDG_CACHE_HOME=${{ runner.temp }}\\killstata-e2e\\cache" >> "$GITHUB_ENV"
            printf '%s\n' "XDG_CONFIG_HOME=${{ runner.temp }}\\killstata-e2e\\config" >> "$GITHUB_ENV"
            printf '%s\n' "XDG_STATE_HOME=${{ runner.temp }}\\killstata-e2e\\state" >> "$GITHUB_ENV"
            printf '%s\n' "MODELS_DEV_API_JSON=${{ github.workspace }}\\packages\\killstata\\test\\tool\\fixtures\\models-api.json" >> "$GITHUB_ENV"
          else
            printf '%s\n' "KILLSTATA_E2E_ROOT=${{ runner.temp }}/killstata-e2e" >> "$GITHUB_ENV"
            printf '%s\n' "KILLSTATA_TEST_HOME=${{ runner.temp }}/killstata-e2e/home" >> "$GITHUB_ENV"
            printf '%s\n' "XDG_DATA_HOME=${{ runner.temp }}/killstata-e2e/share" >> "$GITHUB_ENV"
            printf '%s\n' "XDG_CACHE_HOME=${{ runner.temp }}/killstata-e2e/cache" >> "$GITHUB_ENV"
            printf '%s\n' "XDG_CONFIG_HOME=${{ runner.temp }}/killstata-e2e/config" >> "$GITHUB_ENV"
            printf '%s\n' "XDG_STATE_HOME=${{ runner.temp }}/killstata-e2e/state" >> "$GITHUB_ENV"
            printf '%s\n' "MODELS_DEV_API_JSON=${{ github.workspace }}/packages/killstata/test/tool/fixtures/models-api.json" >> "$GITHUB_ENV"
          fi

      - name: Seed killstata data
        if: matrix.settings.name != 'windows'
        working-directory: packages/killstata
        run: bun script/seed-e2e.ts
        env:
          MODELS_DEV_API_JSON: ${{ env.MODELS_DEV_API_JSON }}
          KILLSTATA_DISABLE_MODELS_FETCH: "true"
          KILLSTATA_DISABLE_SHARE: "true"
          KILLSTATA_DISABLE_LSP_DOWNLOAD: "true"
          KILLSTATA_DISABLE_DEFAULT_PLUGINS: "true"
          KILLSTATA_EXPERIMENTAL_DISABLE_FILEWATCHER: "true"
          KILLSTATA_TEST_HOME: ${{ env.KILLSTATA_TEST_HOME }}
          XDG_DATA_HOME: ${{ env.XDG_DATA_HOME }}
          XDG_CACHE_HOME: ${{ env.XDG_CACHE_HOME }}
          XDG_CONFIG_HOME: ${{ env.XDG_CONFIG_HOME }}
          XDG_STATE_HOME: ${{ env.XDG_STATE_HOME }}
          KILLSTATA_E2E_PROJECT_DIR: ${{ github.workspace }}
          KILLSTATA_E2E_SESSION_TITLE: "E2E Session"
          KILLSTATA_E2E_MESSAGE: "Seeded for UI e2e"
          KILLSTATA_E2E_MODEL: "killstata/gpt-5-nano"

      - name: Run killstata server
        if: matrix.settings.name != 'windows'
        working-directory: packages/killstata
        run: bun dev -- --print-logs --log-level WARN serve --port 4096 --hostname 127.0.0.1 &
        env:
          MODELS_DEV_API_JSON: ${{ env.MODELS_DEV_API_JSON }}
          KILLSTATA_DISABLE_MODELS_FETCH: "true"
          KILLSTATA_DISABLE_SHARE: "true"
          KILLSTATA_DISABLE_LSP_DOWNLOAD: "true"
          KILLSTATA_DISABLE_DEFAULT_PLUGINS: "true"
          KILLSTATA_EXPERIMENTAL_DISABLE_FILEWATCHER: "true"
          KILLSTATA_TEST_HOME: ${{ env.KILLSTATA_TEST_HOME }}
          XDG_DATA_HOME: ${{ env.XDG_DATA_HOME }}
          XDG_CACHE_HOME: ${{ env.XDG_CACHE_HOME }}
          XDG_CONFIG_HOME: ${{ env.XDG_CONFIG_HOME }}
          XDG_STATE_HOME: ${{ env.XDG_STATE_HOME }}
          KILLSTATA_CLIENT: "app"

      - name: Wait for killstata server
        if: matrix.settings.name != 'windows'
        run: |
          for i in {1..120}; do
            curl -fsS "http://127.0.0.1:4096/global/health" > /dev/null && exit 0
            sleep 1
          done
          exit 1

      - name: run
        working-directory: ${{ matrix.settings.workdir }}
        run: ${{ matrix.settings.command }}
        env:
          CI: true
          MODELS_DEV_API_JSON: ${{ env.MODELS_DEV_API_JSON }}
          KILLSTATA_DISABLE_MODELS_FETCH: "true"
          KILLSTATA_DISABLE_SHARE: "true"
          KILLSTATA_DISABLE_LSP_DOWNLOAD: "true"
          KILLSTATA_DISABLE_DEFAULT_PLUGINS: "true"
          KILLSTATA_EXPERIMENTAL_DISABLE_FILEWATCHER: "true"
          KILLSTATA_TEST_HOME: ${{ env.KILLSTATA_TEST_HOME }}
          XDG_DATA_HOME: ${{ env.XDG_DATA_HOME }}
          XDG_CACHE_HOME: ${{ env.XDG_CACHE_HOME }}
          XDG_CONFIG_HOME: ${{ env.XDG_CONFIG_HOME }}
          XDG_STATE_HOME: ${{ env.XDG_STATE_HOME }}
          PLAYWRIGHT_SERVER_HOST: "127.0.0.1"
          PLAYWRIGHT_SERVER_PORT: "4096"
          VITE_KILLSTATA_SERVER_HOST: "127.0.0.1"
          VITE_KILLSTATA_SERVER_PORT: "4096"
          KILLSTATA_CLIENT: "app"
        timeout-minutes: 30
